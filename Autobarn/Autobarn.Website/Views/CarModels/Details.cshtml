@model VehicleModel

@{
	ViewBag.Title = @Model.Name;
}

<h1>@Model.VehicleMake.Name @Model.Name</h1>
<hr />
@if (Model.Vehicles.Any()) {
	<ul>
		@foreach (var vehicle in Model.Vehicles) {
			<li><a href="@Url.Action("Details", "Vehicles", new { id = vehicle.Registration})">@vehicle.Registration</a></li>
		}
	</ul>
} else {
	<p><em>We don't have any @Model.VehicleMake.Name @Model.Name cars for sale right now.</em></p>
}
<hr />
<a href="@Url.Action("Advertise", "Vehicles" , new { id = @Model.Code })">Sell your @Model.VehicleMake.Name @Model.Name here</a>